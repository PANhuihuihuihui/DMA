---
last_mapped_commit: 64e01e3dfea3229b13fc4d25ac8c657efe12dab9
---

# External Integrations

**Analysis Date:** 2026-06-09

## APIs & External Services

**Deployment / Hosting:**
- OpenAI Sites / Cloudflare Worker-compatible static hosting - `README.md` describes a Sites deployment package, `.openai/hosting.json` is copied into `dist/.openai/hosting.json`, and `scripts/prepare-sites-dist.mjs` generates `dist/server/index.js`.
  - SDK/Client: no npm SDK; packaging uses local Node scripts in `scripts/prepare-sites-dist.mjs` and `scripts/package-sites.mjs`.
  - Auth: Not detected in repository files. No `.env` or `.env.*` files detected.
  - Runtime binding: generated worker expects `env.ASSETS` in `dist/server/index.js` produced by `scripts/prepare-sites-dist.mjs`.

**Product-Simulated Channels:**
- TikTok, Instagram, Facebook, Xiaohongshu/RED, and Google Local appear as demo/product channels in `src/main.jsx`, `README.md`, and `docs/landing_page_content_and_design_brief.md`.
  - SDK/Client: Not detected.
  - Auth: Not detected.
  - Network calls: Not detected; these are static/demo concepts, not live integrations.

**AI / Content Generation:**
- The UI presents AI-generated campaign planning, variants, recommendations, and reply drafts in `src/main.jsx`.
  - SDK/Client: Not detected. No `openai`, `anthropic`, or similar AI SDK dependencies are present in `package.json`.
  - Auth: Not detected.
  - Execution: Demo generation is deterministic client-side data shaping in `src/main.jsx`, including `buildPlansFromInput`, `createInitialPlans`, and form handlers that update React state/localStorage.

## Data Storage

**Databases:**
- Not detected.
  - Connection: Not applicable.
  - Client: Not applicable.
- `.openai/hosting.json` declares `"d1": null`, so Cloudflare/OpenAI D1 is explicitly not configured.

**File Storage:**
- Static assets are repository files: generated PNGs in `assets/`, a public screenshot in `public/screenshot.jpeg`, and docs images in `docs/`.
- Packaged deployment uses generated `dist/client/**` static assets and a runtime `env.ASSETS` binding in the worker code generated by `scripts/prepare-sites-dist.mjs`.
- `.openai/hosting.json` declares `"r2": null`, so R2/object storage is explicitly not configured.

**Caching:**
- No external cache detected.
- Browser state persists in `window.localStorage` via `src/main.jsx` keys:
  - `localpilot-language`
  - `localpilot-pilot-request`
  - `localpilot-demo-session`
  - `localpilot-demo-input`
  - `localpilot-demo-plans`
  - `localpilot-demo-active-module`
  - `localpilot-demo-selected-channel`
  - `localpilot-demo-selected-post`
  - `localpilot-demo-selected-inbox`
  - `localpilot-demo-selected-walkthrough`
  - `localpilot-export-package`

## Authentication & Identity

**Auth Provider:**
- Custom demo-only fake login.
  - Implementation: `src/main.jsx` handles the landing page login form with `FormData`, stores a `localpilot-demo-session` object in `window.localStorage`, and navigates to `/app`.
  - Server verification: Not detected.
  - External identity provider: Not detected.
  - Auth SDK: Not detected.

## Monitoring & Observability

**Error Tracking:**
- None detected. No Sentry, Datadog, LogRocket, OpenTelemetry, or similar dependencies are present in `package.json`.

**Logs:**
- Build/package script logging only. `scripts/package-sites.mjs` prints the archive path and size after a successful package.
- Runtime application logging is not a pattern in `src/main.jsx`.

## CI/CD & Deployment

**Hosting:**
- OpenAI Sites / Cloudflare Worker-compatible static hosting artifact.
- `scripts/prepare-sites-dist.mjs` writes a worker that serves `env.ASSETS` and falls back HTML 404s to `/index.html` for client-side routes.
- `scripts/package-sites.mjs` validates required output files and archives `dist` to `SITES_ARCHIVE_PATH` or `/tmp/localpilot-ai-karen-demo-sites.tar.gz`.
- `README.md` states remote Sites deployment waits on Sites enablement for the workspace.

**CI Pipeline:**
- None detected. No GitHub Actions workflow, CI config, or package script for CI was detected in the scanned repository files.

## Environment Configuration

**Required env vars:**
- None required for local app runtime.
- Optional: `SITES_ARCHIVE_PATH` in `scripts/package-sites.mjs` overrides the default archive output path.

**Secrets location:**
- Not detected. No `.env` or `.env.*` files were found during the scan.
- Do not introduce API keys or secrets into `src/main.jsx`, `package.json`, `.openai/hosting.json`, or docs. Add secret-backed integrations through environment configuration and keep secret files uncommitted.

## Webhooks & Callbacks

**Incoming:**
- None detected. There are no API routes, server endpoints, or webhook handlers in `src/`, `scripts/`, or generated deployment source.
- The generated worker in `scripts/prepare-sites-dist.mjs` handles only static asset requests and SPA fallback responses.

**Outgoing:**
- None detected. No `fetch`, Axios, SDK calls, webhook dispatches, or server-side outbound integrations are implemented in source files.
- Demo form submissions in `src/main.jsx` stay local by writing to `window.localStorage`.

---

*Integration audit: 2026-06-09*
