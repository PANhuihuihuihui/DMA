---
phase: 11-carousel-generation
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - backend/app/store.py
  - backend/tests/test_phase11_carousel_generation.py
  - backend/tests/test_phase3_workspace.py
  - scripts/smoke-phase3-screens.mjs
  - src/main.jsx
  - src/styles.css
autonomous: false
requirements:
  - GENC-01
user_setup:
  - service: minimax
    why: "Real carousel image generation and URL-story extraction use MiniMax APIs."
    env_vars:
      - name: MINIMAX_API_KEY
        source: "MiniMax dashboard -> API keys"
must_haves:
  truths:
    - "Merchant can edit only slide-level content inside the existing Creative Editor while the five-slide package stays locked."
    - "The carousel package still follows the existing approval path after slide edits."
    - "The pre-release smoke proves a real Nike URL, real MiniMax generation, and real session-backed credit subtraction."
  artifacts:
    - path: "backend/app/store.py"
      provides: "Carousel editor lock metadata and approval-safe serialization"
      contains: "editorMode"
    - path: "backend/tests/test_phase3_workspace.py"
      provides: "Regression coverage for five-slide carousel editor persistence"
      contains: "test_phase11_carousel_editor_lock"
    - path: "scripts/smoke-phase3-screens.mjs"
      provides: "Frontend smoke for AI Studio carousel success card to Creative Editor handoff"
      contains: "Open in Creative Editor"
    - path: "src/main.jsx"
      provides: "Slide-locked Creative Editor rendering for carousel packages"
      contains: "saveCarouselSlideEdit"
  key_links:
    - from: "src/main.jsx"
      to: "backend/app/store.py"
      via: "updatePhase3Creative and updatePhase3MediaAsset"
      pattern: "updatePhase3Creative|updatePhase3MediaAsset"
    - from: "backend/tests/test_phase11_carousel_generation.py"
      to: "backend/app/store.py"
      via: "editor handoff and approval metadata assertions"
      pattern: "editorMode|slideRoles|approval"
    - from: "backend/tests/test_phase3_workspace.py"
      to: "src/main.jsx"
      via: "existing Creative Editor save path assumptions"
      pattern: "mediaAssets"
---

<objective>
Lock the Creative Editor and approval behavior around the generated carousel package, then finish the phase with the required real-provider smoke gate using a real Nike URL and real ledger subtraction, per D-09, D-10, D-16, D-18, and D-20.

Phase Goal:
**As a** merchant, **I want to** make slide-level edits inside the existing Creative Editor and then approve the package, **so that** the generated carousel stays brand-locked and publish-safe.

Purpose: Finish the operator-safe handoff and prove the real MiniMax plus credit-ledger path before release.
Output: Slide-locked editor behavior, approval continuity checks, and a blocking Nike/MiniMax/ledger smoke gate.
</objective>

<execution_context>
@/Users/huijie/.codex/gsd-core/workflows/execute-plan.md
@/Users/huijie/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-carousel-generation/11-CONTEXT.md
@.planning/phases/11-carousel-generation/11-RESEARCH.md
@.planning/phases/11-carousel-generation/11-PATTERNS.md
@.planning/phases/11-carousel-generation/11-UI-SPEC.md
@.planning/phases/03-local-campaign-draft-workbench/03-01-SUMMARY.md
@backend/app/store.py
@backend/tests/test_phase11_carousel_generation.py
@backend/tests/test_phase3_workspace.py
@src/main.jsx
@src/styles.css
</context>

## Artifacts This Phase Produces

- `backend/app/store.py::editorMode = "carousel_slide_edit"`
- `backend/app/store.py::lockedLayout`
- `backend/app/store.py::composedPayload`
- `backend/tests/test_phase3_workspace.py::test_phase11_carousel_editor_lock`
- `src/main.jsx::saveCarouselSlideEdit`
- `src/main.jsx::CarouselEditorPanel`

<tasks>

<task type="auto">
  <name>Task 1: Keep the carousel package slide-locked through Creative Editor saves and downstream approval</name>
  <files>backend/app/store.py, backend/tests/test_phase11_carousel_generation.py, backend/tests/test_phase3_workspace.py, scripts/smoke-phase3-screens.mjs, src/main.jsx, src/styles.css</files>
  <read_first>
backend/app/store.py
backend/tests/test_phase11_carousel_generation.py
backend/tests/test_phase3_workspace.py
src/main.jsx
src/styles.css
.planning/phases/11-carousel-generation/11-UI-SPEC.md
.planning/phases/03-local-campaign-draft-workbench/03-01-SUMMARY.md
  </read_first>
  <acceptance_criteria>
    - Carousel creatives serialize explicit lock metadata for slide count, slide order, aspect ratio, preset, logo placement, and brand lock.
    - Carousel creatives keep per-slide `composedPayload` fields for headline/body/CTA, logo slot, layout tokens, and preview render data after slide edits.
    - The Creative Editor shows five numbered slide thumbnails and only slide-level edit actions for carousel packages.
    - Automated screen smoke proves the normalized carousel payload renders as a five-slide branded package in the success card and then opens the same package in Creative Editor with the saved copy/layout/logo-slot composition visible.
    - Existing approval-facing fields remain present after carousel edits, and no UI control appears for add/delete/reorder/aspect/preset/logo unlock.
  </acceptance_criteria>
  <action>Extend the creative and asset metadata created by `materialize_carousel_package` so carousel packages carry `editorMode = "carousel_slide_edit"` and immutable `lockedLayout = { slideCount, slideRoles, aspectRatio, presetId, logoPlacement, brandLock }` per D-16, D-18, and D-20. Keep those keys stable when `update_generated_creative()` and `update_creative_media_asset()` persist edits. Preserve each slide's `composedPayload = { headline, body, ctaLabel, logoSlot, layout, thumbnailRender, editorPreview }` so the existing Creative Editor renders branded composed slides rather than five raw images with metadata attached. In `src/main.jsx` and `src/styles.css`, detect `editorMode === "carousel_slide_edit"` and render the existing Creative Editor as a five-thumbnail left rail, an active slide canvas, and a slide-level inspector that edits only headline, body, CTA, and image replacement while reusing the persisted `composedPayload` render path. Use the existing save paths, keep `Save slide edits` as the primary action, preserve `needs_review`, and leave approval on the current exact-version workflow instead of adding a second approval mechanism. Extend `backend/tests/test_phase11_carousel_generation.py` and `backend/tests/test_phase3_workspace.py` so one carousel creative has exactly five ordered slides, slide edits persist through the existing save helpers, immutable layout keys do not change after save, composed payload preview fields remain renderable, and approval-facing fields remain populated. Extend `scripts/smoke-phase3-screens.mjs` so it covers the Phase 11 AI Studio carousel flow: launch or hydrate a deterministic carousel package, assert the success card shows five ordered branded thumbnails with slide copy preview, click `Open in Creative Editor`, and assert the existing editor renders the same package with five thumbnails plus the saved headline/body/CTA/logo-slot composition.</action>
  <verify>
    <automated>python3 -m unittest backend.tests.test_phase11_carousel_generation backend.tests.test_phase3_workspace -v && npm run test:phase3-screens && npm run build</automated>
  </verify>
  <done>The carousel package stays locked to five ordered slides through Creative Editor saves and can still move through the existing approval surface.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the real Nike URL plus MiniMax plus ledger smoke before release</name>
  <files>Verification only — no file modifications in this checkpoint task.</files>
  <read_first>
.planning/phases/11-carousel-generation/11-CONTEXT.md
.planning/phases/11-carousel-generation/11-UI-SPEC.md
src/main.jsx
backend/app/store.py
  </read_first>
  <action>Use the implemented AI Studio carousel flow with a real authenticated session and the normal session-backed merchant or test-account ledger per D-09 and D-10. Do not use `dispatch: false`, direct SQL writes, anonymous merchant fallback, or free internal credit bypasses. The smoke input must be a real Nike URL such as `https://www.nike.com/`, the image path must be MiniMax-backed, and the run must end through the same Creative Editor handoff used by the shipped UI.</action>
  <what-built>Carousel idea and URL generation, MiniMax adapter routing, five-slide editor handoff, composed brand-layout slide payloads, and slide-locked Creative Editor behavior from Plans 11-01 and 11-02 plus Task 1.</what-built>
  <how-to-verify>
1. Start the backend and frontend with `MINIMAX_API_KEY` configured.
2. Sign in through the normal LocalPilot session flow or backend dev-login so the run uses a real session-backed merchant/test-account ledger.
3. Run the idea flow first: open `AI Studio`, enter one written idea, launch the carousel, and confirm the success card shows five ordered thumbnails rendered from the stored composed payload plus `Open in Creative Editor`.
4. Confirm the Creative Editor preview uses persisted headline/body/CTA/logo slot/layout data, not raw image-only rendering, and still exposes only slide-level edit actions.
5. Return to `AI Studio`, switch to the carousel flow, choose `Paste public URL`, and use `https://www.nike.com/`.
6. Confirm the setup card shows the canonical five-slide order, brand-lock messaging, MiniMax as the default model, and the exact credit cost before launch.
7. Launch the URL carousel and wait for `Ready in editor`; before clicking through, confirm the success card shows one package with five ordered branded thumbnails plus visible role/headline preview rather than five generic job cards.
8. Open the Creative Editor and confirm it opens one creative with five ordered slide thumbnails, only slide-level edit actions, and the saved headline/body/CTA plus fixed logo-slot composition visible on the active slide.
9. Confirm available credits decreased by the catalog cost through the normal generation ledger path and the URL did not rewrite the saved brand colors, typography, or logo behavior.
  </how-to-verify>
  <verify>
    <automated>python3 -m unittest backend.tests.test_phase11_carousel_generation backend.tests.test_phase3_workspace -v && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The idea flow success card and editor handoff prove the frontend renders the normalized composed payload path directly.
    - The smoke uses a real Nike URL and a real MiniMax-backed image path.
    - The run consumes credits through the normal session-backed generation ledger with no bypass.
    - The finished package opens as one creative with five ordered slides, keeps the saved brand lock, and remains approval-ready.
  </acceptance_criteria>
  <done>The manual release gate confirms the shipped carousel flow uses the real Nike URL, real MiniMax generation, and the normal ledger-backed credit path.</done>
  <resume-signal>Type "approved" or describe the smoke failure with the observed job ID, credit delta, and editor behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Creative Editor save actions | Untrusted edit payloads can try to unlock layout, slide count, or approval state. |
| client -> session-backed generation ledger | Release smoke must prove a normal authenticated ledger path rather than an anonymous or bypass route. |
| backend -> approval workflow | Carousel edits must remain compatible with exact-version approval semantics already in production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | `update_generated_creative` / `update_creative_media_asset` | mitigate | Persist immutable carousel lock metadata and ignore client attempts to change slide count, order, aspect ratio, preset, or logo placement. |
| T-11-03-02 | Elevation of Privilege | manual smoke / merchant resolution | mitigate | Require a real session-backed merchant or test-account ledger path for the Nike smoke; do not allow anonymous demo fallback for release validation. |
| T-11-03-03 | Repudiation | approval handoff | mitigate | Keep approval on the existing exact-version workflow so post-edit package state remains auditable and versioned. |
| T-11-03-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs are planned; reuse stdlib HTTP plus current repo dependencies only. |
</threat_model>

<verification>
- `python3 -m unittest backend.tests.test_phase11_carousel_generation backend.tests.test_phase3_workspace -v`
- `npm run test:phase3-screens`
- `npm run build`
- Human smoke: complete Task 2 with a real Nike URL, real MiniMax output, and a real session-backed ledger deduction
</verification>

<success_criteria>
- Carousel packages remain locked to five ordered slides with slide-level edits only.
- Approval still flows through the existing exact-version path after carousel edits.
- The release gate proves the real Nike URL plus MiniMax plus ledger path end to end before Phase 11 is marked complete.
</success_criteria>

<output>
Create `.planning/phases/11-carousel-generation/11-03-SUMMARY.md` when done
</output>
