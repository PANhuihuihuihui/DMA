---
phase: 05-tiktok-upload-and-direct-post-gates
plan: 02
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - backend/app/store.py
  - backend/app/server.py
  - backend/app/contracts.py
  - backend/app/facebook_oauth.py
  - src/api/publishingClient.js
  - backend/tests/test_channel_health.py
  - backend/tests/test_channel_disconnect.py
autonomous: true
requirements: [ACCT-04, ACCT-05, ACCT-06, ACCT-07]
must_haves:
  truths:
    - "Facebook and TikTok each expose explicit connection health states: connected, missing_permission, expired_token, review_blocked, reconnect_required, disconnected"
    - "Content creation and editing remain available even when a channel is not connected; only scheduling and publish actions for that channel are blocked"
    - "Disconnecting a channel immediately prevents new schedule or publish progression for that channel without deleting historical drafts, approvals, or jobs"
    - "Server-side gates enforce channel status checks even if the UI is stale or bypassed"
  artifacts:
    - path: "backend/tests/test_channel_health.py"
      provides: "Health-state coverage for Facebook and TikTok connected channels"
      contains: "reconnect_required"
    - path: "backend/tests/test_channel_disconnect.py"
      provides: "Disconnect behavior and scheduling/publish gate regression coverage"
      contains: "disconnected"
    - path: "backend/app/server.py"
      provides: "Channel status and disconnect endpoints with server-enforced gates"
      contains: "disconnect"
  key_links:
    - from: "backend/app/server.py"
      to: "backend/app/store.py"
      via: "channel health reads and disconnect mutation"
      pattern: "connected_channel"
    - from: "src/api/publishingClient.js"
      to: "backend/app/server.py"
      via: "health fetch and disconnect actions"
      pattern: "channel"
---

<objective>
Add first-class per-channel health visibility and disconnect behavior for Facebook and TikTok on top of the multi-channel foundation.

Purpose: satisfy ACCT-04..07 while preserving the core decision that a merchant is never blocked from building content because one channel is unhealthy.
Output: backend health/disconnect support, channel-gated schedule/publish enforcement, frontend client calls, and tests.
</objective>

<execution_context>
@/Users/huijie/.codex/gsd-core/workflows/execute-plan.md
@/Users/huijie/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tiktok-upload-and-direct-post-gates/05-CONTEXT.md
@.planning/phases/05-tiktok-upload-and-direct-post-gates/05-01-PLAN.md
@backend/app/store.py
@backend/app/server.py
@backend/app/contracts.py
@backend/app/facebook_oauth.py
@src/api/publishingClient.js

This plan consumes the `sessions`, `channel_registry`, `scheduled_posts`, and `connected_channels` extensions introduced by 05-01.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add normalized health-state persistence and read APIs for Facebook and TikTok</name>
  <files>backend/app/store.py, backend/app/server.py, backend/app/contracts.py, backend/app/facebook_oauth.py, backend/tests/test_channel_health.py</files>
  <read_first>
    - .planning/phases/05-tiktok-upload-and-direct-post-gates/05-CONTEXT.md (D-01, D-02, D-04)
    - backend/app/facebook_oauth.py
    - backend/app/store.py
    - backend/app/server.py
  </read_first>
  <action>
  Normalize channel health to the canonical status set `connected`, `missing_permission`, `expired_token`, `review_blocked`, `reconnect_required`, `disconnected`. Reuse the existing token-boundary and Facebook health seams where possible, and make the backend return a per-channel health payload that the frontend can consume for Facebook and TikTok. The read path should resolve the current session, scope to the merchant, and expose enough diagnostics to distinguish permission loss, expired credentials, creator-review/app-review blocks, and reconnect-needed states without exposing secrets.
  </action>
  <verify>
    <automated>python3 -m unittest backend.tests.test_channel_health -v</automated>
  </verify>
  <acceptance_criteria>
    - Both Facebook and TikTok channels can be read with the canonical health states.
    - Backend health responses are merchant-scoped and redact token/provider secrets.
    - Unknown or stale states are normalized into one of the documented statuses.
  </acceptance_criteria>
  <done>Per-channel health is persisted and readable through a stable backend contract for Facebook and TikTok.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce disconnect and channel-specific schedule/publish gates without blocking content creation</name>
  <files>backend/app/store.py, backend/app/server.py, src/api/publishingClient.js, backend/tests/test_channel_disconnect.py</files>
  <read_first>
    - .planning/phases/05-tiktok-upload-and-direct-post-gates/05-CONTEXT.md (D-02, D-03, D-04, D-21)
    - backend/app/server.py
    - src/api/publishingClient.js
    - backend/app/store.py
  </read_first>
  <action>
  Add a disconnect mutation for connected channels that marks the channel `disconnected` or inactive immediately while preserving historical records. Enforce channel health in the backend schedule/publish progression path so a post on a disconnected, expired, missing-permission, review-blocked, or reconnect-required channel cannot move into queueing or publishing. Keep content creation and draft editing available regardless of channel health. Mirror the new read/mutation paths in `src/api/publishingClient.js`.
  </action>
  <verify>
    <automated>python3 -m unittest backend.tests.test_channel_disconnect -v</automated>
  </verify>
  <acceptance_criteria>
    - Disconnecting Facebook or TikTok blocks new schedule/publish progression for that channel immediately.
    - Existing drafts, approvals, publish jobs, and historical channel rows remain intact after disconnect.
    - Content creation/editing APIs remain usable for disconnected or unhealthy channels.
    - Backend enforcement works even if a client attempts to bypass UI gating.
  </acceptance_criteria>
  <done>Channel disconnect and health-gated schedule/publish behavior are enforced server-side while content authoring remains available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser session → backend | Channel health and disconnect actions must be scoped to the authenticated merchant session |
| unhealthy channel → publish pipeline | Unhealthy channels must not allow new queueing or publish progression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Elevation of Privilege | channel disconnect | mitigate | Resolve session, scope by merchant, reject cross-merchant channel mutation |
| T-05-02-02 | Tampering | stale UI bypass | mitigate | Re-check channel status server-side before schedule or publish progression |
| T-05-02-03 | Information Disclosure | health diagnostics | mitigate | Expose normalized status and safe diagnostics only; no tokens or raw provider payloads |
</threat_model>

<verification>
- `python3 -m unittest backend.tests.test_channel_health backend.tests.test_channel_disconnect -v`
- Targeted regressions for existing publish routes that now depend on channel health
</verification>

<success_criteria>
- Merchant can see explicit Facebook and TikTok channel health states.
- Disconnecting a channel blocks new schedule/publish work for that channel immediately.
- Content creation and editing remain available when a channel is unhealthy or disconnected.
- Backend gates, not just UI warnings, enforce channel-specific restrictions.
</success_criteria>

<output>
Create `.planning/phases/05-tiktok-upload-and-direct-post-gates/05-02-SUMMARY.md` when done.
</output>
