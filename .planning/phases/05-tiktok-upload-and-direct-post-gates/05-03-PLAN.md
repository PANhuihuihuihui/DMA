---
phase: 05-tiktok-upload-and-direct-post-gates
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - backend/app/store.py
  - backend/app/contracts.py
  - backend/app/server.py
  - src/api/publishingClient.js
  - backend/tests/test_tiktok_creator_info.py
  - backend/tests/test_tiktok_disclosure_gates.py
autonomous: true
requirements: [TT-01, TT-02, TT-03]
must_haves:
  truths:
    - "TikTok creator settings are fetched at connect or refresh time and persisted as a versioned snapshot on the connected channel"
    - "Approval snapshots for TikTok embed the relevant creator-info snapshot so later publish attempts validate against what the merchant approved"
    - "TikTok approval requires explicit disclosure, privacy, and interaction confirmations before submission"
    - "Creator-info mismatches or missing confirmations block approval or publish progression for TikTok only"
  artifacts:
    - path: "backend/tests/test_tiktok_creator_info.py"
      provides: "Creator snapshot persistence and approval embedding coverage"
      contains: "creatorInfoSnapshot"
    - path: "backend/tests/test_tiktok_disclosure_gates.py"
      provides: "Disclosure/privacy/interaction confirmation gate coverage"
      contains: "disclosure"
    - path: "backend/app/contracts.py"
      provides: "Approval snapshot contract extended for TikTok creator settings"
      contains: "creator"
  key_links:
    - from: "backend/app/contracts.py"
      to: "backend/app/store.py"
      via: "TikTok approval snapshot persistence"
      pattern: "snapshot"
    - from: "src/api/publishingClient.js"
      to: "backend/app/server.py"
      via: "creator-info fetch and approval submit payload"
      pattern: "creator"
---

<objective>
Add TikTok creator-info persistence and explicit disclosure confirmation gates to the approval path.

Purpose: satisfy TT-01..03 while keeping creator settings backend-authoritative and approval snapshots stable over time.
Output: creator-info storage, snapshot-aware approval contracts, disclosure confirmation enforcement, and tests.
</objective>

<execution_context>
@/Users/huijie/.codex/gsd-core/workflows/execute-plan.md
@/Users/huijie/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tiktok-upload-and-direct-post-gates/05-CONTEXT.md
@.planning/phases/05-tiktok-upload-and-direct-post-gates/05-01-PLAN.md
@.planning/phases/05-tiktok-upload-and-direct-post-gates/05-02-PLAN.md
@backend/app/store.py
@backend/app/contracts.py
@backend/app/server.py
@src/api/publishingClient.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist TikTok creator-info snapshots and attach them to approval snapshots</name>
  <files>backend/app/store.py, backend/app/contracts.py, backend/app/server.py, backend/tests/test_tiktok_creator_info.py</files>
  <read_first>
    - .planning/phases/05-tiktok-upload-and-direct-post-gates/05-CONTEXT.md (D-05, D-07)
    - backend/app/contracts.py
    - backend/app/store.py
    - backend/app/server.py
  </read_first>
  <action>
  Add storage and serialization for a versioned TikTok creator-info snapshot on the connected channel, populated at connect and refresh time. Extend the TikTok approval snapshot payload to freeze the creator settings that matter for later publish eligibility, including disclosure/privacy/interaction defaults or constraints returned by TikTok APIs. Keep this data redacted and bounded to what is needed for approval and publish validation.
  </action>
  <verify>
    <automated>python3 -m unittest backend.tests.test_tiktok_creator_info -v</automated>
  </verify>
  <acceptance_criteria>
    - TikTok creator-info snapshots are persisted and versioned.
    - Approval snapshots include the creator-info state needed to validate later publish attempts.
    - Refreshing creator settings produces an updated snapshot without mutating historical approval snapshots.
  </acceptance_criteria>
  <done>TikTok creator settings are stored in a stable, approval-linked shape.</done>
</task>

<task type="auto">
  <name>Task 2: Require disclosure, privacy, and interaction confirmations before TikTok approval</name>
  <files>backend/app/server.py, src/api/publishingClient.js, backend/tests/test_tiktok_disclosure_gates.py</files>
  <read_first>
    - .planning/phases/05-tiktok-upload-and-direct-post-gates/05-CONTEXT.md (D-06, D-07)
    - backend/app/server.py
    - src/api/publishingClient.js
  </read_first>
  <action>
  Add backend-authoritative approval validation for TikTok drafts so required disclosure, privacy, and interaction confirmations must be present and internally consistent with the stored creator snapshot. Wire the client request shape through `src/api/publishingClient.js`, but keep the backend as the final gate. Reject approval submission when confirmations are missing, mismatched, or stale relative to the creator snapshot.
  </action>
  <verify>
    <automated>python3 -m unittest backend.tests.test_tiktok_disclosure_gates -v</automated>
  </verify>
  <acceptance_criteria>
    - TikTok approval cannot be submitted without the required confirmations.
    - Mismatch between the current creator snapshot and submitted confirmations is rejected.
    - Facebook approval behavior is unchanged.
  </acceptance_criteria>
  <done>TikTok approvals now require explicit disclosure-related confirmations and validate them against stored creator settings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TikTok creator-info APIs → backend snapshot | External creator settings must be frozen safely for approval and publish validation |
| browser approval form → backend approval gate | Merchant-supplied confirmations must be validated against stored backend state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | approval confirmations | mitigate | Validate submitted confirmations against the stored creator snapshot |
| T-05-03-02 | Repudiation | disclosure approvals | mitigate | Freeze creator snapshot and confirmation data into the approval snapshot |
| T-05-03-03 | Information Disclosure | creator settings | mitigate | Persist only the settings required for approval/publish decisions and redact sensitive provider payloads |
</threat_model>

<verification>
- `python3 -m unittest backend.tests.test_tiktok_creator_info backend.tests.test_tiktok_disclosure_gates -v`
- Targeted approval snapshot regression checks in existing backend tests
</verification>

<success_criteria>
- TikTok creator settings are persisted as versioned snapshots.
- TikTok approval snapshots embed the creator state needed for later publish validation.
- Approval is blocked when required disclosure, privacy, or interaction confirmations are missing or stale.
</success_criteria>

<output>
Create `.planning/phases/05-tiktok-upload-and-direct-post-gates/05-03-SUMMARY.md` when done.
</output>
