---
phase: 05-tiktok-upload-and-direct-post-gates
plan: 05
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
files_modified:
  - backend/app/store.py
  - backend/app/contracts.py
  - backend/app/server.py
  - backend/app/tiktok_publisher.py
  - src/api/publishingClient.js
  - backend/tests/test_tiktok_media_validation.py
  - backend/tests/test_tiktok_failure_taxonomy.py
autonomous: true
requirements: [TT-07, MEDIA-04]
must_haves:
  truths:
    - "TikTok media validation runs in the backend before publish jobs are created"
    - "Media failures are classified into at least authentication, scope, creator-setting mismatch, media_validation, rate_limit, audit_or_visibility_block, platform_transient, and unknown"
    - "Media validation reasons include file type, size, duration, URL accessibility, and format compatibility"
    - "Generated media defaults to the strictest relevant channel constraints first, and additional variants are generated only when needed for compliance or measurable performance reasons"
  artifacts:
    - path: "backend/tests/test_tiktok_media_validation.py"
      provides: "Backend-authoritative TikTok media validation coverage"
      contains: "media_validation"
    - path: "backend/tests/test_tiktok_failure_taxonomy.py"
      provides: "Publish failure classification coverage"
      contains: "rate_limit"
    - path: "backend/app/tiktok_publisher.py"
      provides: "TikTok media gate and failure taxonomy integration"
      contains: "validation"
  key_links:
    - from: "backend/app/tiktok_publisher.py"
      to: "backend/app/store.py"
      via: "classified diagnostics and pre-job validation"
      pattern: "diagnostic"
    - from: "src/api/publishingClient.js"
      to: "backend/app/server.py"
      via: "validation and failure surfaces"
      pattern: "validation"
---

<objective>
Add backend-authoritative TikTok media validation and a stable failure taxonomy, including generation defaults that target the strictest relevant channel constraints first.

Purpose: satisfy TT-07 and MEDIA-04 while avoiding avoidable re-renders and unbounded variant generation.
Output: validation policies, classified diagnostics, client-facing validation surfaces, and tests.
</objective>

<execution_context>
@/Users/huijie/.codex/gsd-core/workflows/execute-plan.md
@/Users/huijie/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tiktok-upload-and-direct-post-gates/05-CONTEXT.md
@.planning/phases/05-tiktok-upload-and-direct-post-gates/05-03-PLAN.md
@.planning/phases/05-tiktok-upload-and-direct-post-gates/05-04-PLAN.md
@backend/app/store.py
@backend/app/contracts.py
@backend/app/server.py
@backend/app/tiktok_publisher.py
@src/api/publishingClient.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend-authoritative TikTok media validation before publish-job creation</name>
  <files>backend/app/tiktok_publisher.py, backend/app/server.py, backend/app/contracts.py, backend/tests/test_tiktok_media_validation.py</files>
  <read_first>
    - .planning/phases/05-tiktok-upload-and-direct-post-gates/05-CONTEXT.md (D-11, D-12, D-13)
    - backend/app/tiktok_publisher.py
    - backend/app/contracts.py
    - backend/app/server.py
  </read_first>
  <action>
  Add a backend validation step for TikTok media before any publish job is created. The validator should classify failures for file type, size, duration, URL accessibility, and format compatibility, and should return a stable payload shape that later support and analytics layers can consume. Frontend checks may remain optimistic, but the backend is the source of truth.
  </action>
  <verify>
    <automated>python3 -m unittest backend.tests.test_tiktok_media_validation -v</automated>
  </verify>
  <acceptance_criteria>
    - Invalid TikTok media is rejected before a publish job is created.
    - Validation surfaces the documented failure reasons with stable classification.
    - Frontend optimism does not bypass backend validation.
  </acceptance_criteria>
  <done>TikTok media is validated in the backend before queueing or publishing work begins.</done>
</task>

<task type="auto">
  <name>Task 2: Classify TikTok failures and enforce strictest-channel media generation defaults</name>
  <files>backend/app/store.py, backend/app/tiktok_publisher.py, src/api/publishingClient.js, backend/tests/test_tiktok_failure_taxonomy.py</files>
  <read_first>
    - .planning/phases/05-tiktok-upload-and-direct-post-gates/05-CONTEXT.md (D-12, D-13)
    - backend/app/store.py
    - backend/app/tiktok_publisher.py
  </read_first>
  <action>
  Normalize TikTok publish failures into the required taxonomy and persist them in provider diagnostics and analytics-friendly fields. Ensure media generation defaults to assets that satisfy the strictest relevant channel constraints first; only create extra variants when there is a concrete compliance or performance reason to do so. Do not let variant generation become a default fallback for avoidable validation misses.
  </action>
  <verify>
    <automated>python3 -m unittest backend.tests.test_tiktok_failure_taxonomy -v</automated>
  </verify>
  <acceptance_criteria>
    - Publish failures map to the documented taxonomy with stable backend classifications.
    - Diagnostics distinguish media-validation failures from auth, scope, creator-setting, rate-limit, audit/visibility, transient, and unknown failures.
    - Media generation policy reflects the "strictest relevant channel first, variants only when needed" rule.
  </acceptance_criteria>
  <done>TikTok failures are classified consistently and media generation defaults align with the locked phase decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated media → backend publish validation | Invalid media must be stopped before any publish job is created |
| provider failures → diagnostics/analytics | Failure classes must be stable, useful, and safe to inspect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | client-side validation bypass | mitigate | Re-run media validation in the backend before job creation |
| T-05-05-02 | Repudiation | ambiguous publish failures | mitigate | Persist stable failure classes and reason details in diagnostics |
| T-05-05-03 | Denial of Service | variant explosion | mitigate | Default to strictest-channel-compliant assets and generate extra variants only when justified |
</threat_model>

<verification>
- `python3 -m unittest backend.tests.test_tiktok_media_validation backend.tests.test_tiktok_failure_taxonomy -v`
- Targeted TikTok publish flow regressions with validation enabled
</verification>

<success_criteria>
- TikTok media is validated in the backend before publish jobs are created.
- Failure classes cleanly distinguish media issues from auth, scope, creator-setting, rate-limit, audit/visibility, transient, and unknown failures.
- Media generation defaults target the strictest relevant channel constraints, with extra variants only when needed.
</success_criteria>

<output>
Create `.planning/phases/05-tiktok-upload-and-direct-post-gates/05-05-SUMMARY.md` when done.
</output>
