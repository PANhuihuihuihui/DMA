---
phase: 05-tiktok-upload-and-direct-post-gates
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - backend/app/server.py
  - backend/app/store.py
  - backend/app/contracts.py
  - backend/app/tiktok_publisher.py
  - src/api/publishingClient.js
  - backend/tests/test_tiktok_publish_routes.py
  - backend/tests/test_tiktok_direct_post_gate.py
autonomous: true
requirements: [TT-04, TT-05, TT-06]
must_haves:
  truths:
    - "Approved TikTok drafts default to the safer upload-to-inbox or draft-style route for v1"
    - "Route selection is backend-owned per publish attempt and stored in diagnostics or a dedicated field"
    - "Direct Post is available only when official eligibility, scopes, creator settings, disclosure confirmations, and app-review gates are all satisfied"
    - "TikTok publish attempts create publish jobs, attempts, events, outcomes, and provider diagnostics consistent with the existing publish lifecycle"
  artifacts:
    - path: "backend/app/tiktok_publisher.py"
      provides: "TikTok publish route orchestration and provider diagnostics"
      contains: "direct_post"
    - path: "backend/tests/test_tiktok_publish_routes.py"
      provides: "Upload-to-inbox or draft-style route coverage"
      contains: "upload"
    - path: "backend/tests/test_tiktok_direct_post_gate.py"
      provides: "Direct Post eligibility gate coverage"
      contains: "eligibility"
  key_links:
    - from: "backend/app/tiktok_publisher.py"
      to: "backend/app/store.py"
      via: "publish job, attempt, event, and outcome writes"
      pattern: "publish"
    - from: "backend/app/server.py"
      to: "backend/app/tiktok_publisher.py"
      via: "publish endpoint dispatch"
      pattern: "tiktok"
---

<objective>
Implement TikTok publish delivery through official upload or draft-style routes and gate Direct Post behind all required official checks.

Purpose: satisfy TT-04..06 while keeping route selection backend-authoritative and reducing v1 risk through upload-first delivery.
Output: TikTok publisher module, publish endpoints, route diagnostics, Direct Post gate checks, and tests.
</objective>

<execution_context>
@/Users/huijie/.codex/gsd-core/workflows/execute-plan.md
@/Users/huijie/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tiktok-upload-and-direct-post-gates/05-CONTEXT.md
@.planning/phases/05-tiktok-upload-and-direct-post-gates/05-01-PLAN.md
@.planning/phases/05-tiktok-upload-and-direct-post-gates/05-02-PLAN.md
@.planning/phases/05-tiktok-upload-and-direct-post-gates/05-03-PLAN.md
@backend/app/server.py
@backend/app/store.py
@backend/app/contracts.py
@src/api/publishingClient.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement TikTok upload or draft-style publishing with normalized lifecycle writes</name>
  <files>backend/app/tiktok_publisher.py, backend/app/server.py, backend/app/store.py, backend/app/contracts.py, backend/tests/test_tiktok_publish_routes.py</files>
  <read_first>
    - .planning/phases/05-tiktok-upload-and-direct-post-gates/05-CONTEXT.md (D-08, D-09)
    - backend/app/facebook_publisher.py
    - backend/app/store.py
    - backend/app/contracts.py
  </read_first>
  <action>
  Add a TikTok publisher path that publishes approved snapshots through the official upload-to-inbox or draft-style flow by default. Mirror the existing publish lifecycle conventions by writing publish jobs, attempts, events, outcomes, and safe diagnostics. Route selection must be decided in the backend per attempt and persisted in a way support and later phases can inspect.
  </action>
  <verify>
    <automated>python3 -m unittest backend.tests.test_tiktok_publish_routes -v</automated>
  </verify>
  <acceptance_criteria>
    - Approved TikTok drafts can be delivered through an official upload or draft-style path.
    - Route choice is recorded per publish attempt.
    - Publish lifecycle records remain consistent with the existing Facebook-oriented pipeline.
  </acceptance_criteria>
  <done>TikTok drafts can be delivered through the official low-risk route with full lifecycle records.</done>
</task>

<task type="auto">
  <name>Task 2: Gate Direct Post behind all official eligibility checks</name>
  <files>backend/app/tiktok_publisher.py, backend/app/server.py, src/api/publishingClient.js, backend/tests/test_tiktok_direct_post_gate.py</files>
  <read_first>
    - .planning/phases/05-tiktok-upload-and-direct-post-gates/05-CONTEXT.md (D-09, D-10)
    - backend/app/server.py
    - src/api/publishingClient.js
  </read_first>
  <action>
  Add an explicit Direct Post eligibility gate that only allows the route when app-review or app-audit requirements, required scopes, creator-settings compatibility, disclosure confirmations, and channel health all pass. When the gate fails, the backend should fall back to the safer upload or draft-style route where allowed, or reject the publish attempt with classified diagnostics when no official route is available.
  </action>
  <verify>
    <automated>python3 -m unittest backend.tests.test_tiktok_direct_post_gate -v</automated>
  </verify>
  <acceptance_criteria>
    - Direct Post is unavailable unless all documented official gates pass.
    - Route fallback behavior is deterministic and backend-controlled.
    - Diagnostics explain why Direct Post was blocked without leaking secrets.
  </acceptance_criteria>
  <done>Direct Post is opt-in and tightly gated, while upload-first delivery remains the default path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved TikTok snapshot → TikTok publish route | Backend must ensure the chosen route is official, eligible, and auditable |
| Direct Post gate → provider APIs | Direct Post must not be attempted when app-review, scope, or creator constraints are unmet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Elevation of Privilege | Direct Post route | mitigate | Gate route selection on all official eligibility checks server-side |
| T-05-04-02 | Tampering | publish route selection | mitigate | Keep route selection backend-owned and persist the chosen route in diagnostics |
| T-05-04-03 | Repudiation | TikTok publish attempts | mitigate | Record jobs, attempts, events, outcomes, and route diagnostics per attempt |
</threat_model>

<verification>
- `python3 -m unittest backend.tests.test_tiktok_publish_routes backend.tests.test_tiktok_direct_post_gate -v`
- Existing publish lifecycle suite with TikTok additions
</verification>

<success_criteria>
- Approved TikTok drafts can go through official upload or draft-style delivery.
- Direct Post remains blocked unless all official eligibility conditions pass.
- Publish diagnostics make the selected route and gating outcome inspectable.
</success_criteria>

<output>
Create `.planning/phases/05-tiktok-upload-and-direct-post-gates/05-04-SUMMARY.md` when done.
</output>
