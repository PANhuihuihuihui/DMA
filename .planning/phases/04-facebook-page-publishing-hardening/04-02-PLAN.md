---
phase: 04-facebook-page-publishing-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/facebook-app-review-evidence.md
autonomous: true
requirements: [FB-01]
user_setup:
  - service: meta-developer-app
    why: "App review evidence must reflect the real Meta app config (FB-01)"
    env_vars:
      - name: FACEBOOK_APP_ID
        source: "Meta App Dashboard → Settings → Basic"
      - name: FACEBOOK_APP_SECRET
        source: "Meta App Dashboard → Settings → Basic"
      - name: FACEBOOK_REDIRECT_URI
        source: "Meta App Dashboard → Facebook Login → Settings → Valid OAuth Redirect URIs"
    dashboard_config:
      - task: "Confirm requested permissions (pages_show_list, pages_read_engagement, pages_manage_posts) and Advanced Access / Business Verification status"
        location: "Meta App Dashboard → App Review → Permissions and Features"

must_haves:
  truths:
    - "A reader can see exactly which Meta app config, scopes, and review steps are needed to publish to a Facebook Page in production"
    - "The doc names pages_manage_posts as requiring App Review + Business Verification + Advanced Access for non-owned Pages"
    - "The doc includes a screencast checklist covering connect → select Page → approve → publish"
  artifacts:
    - path: "docs/facebook-app-review-evidence.md"
      provides: "Meta app/permissions/test-Page/screencast evidence for FB-01"
      min_lines: 60
      contains: "pages_manage_posts"
  key_links:
    - from: "docs/facebook-app-review-evidence.md"
      to: "backend/app/facebook_oauth.py"
      via: "documents the REQUIRED_SCOPES + redirect URI the code actually requests"
      pattern: "pages_manage_posts"
---

<objective>
Produce the Meta app-review evidence artifact (FB-01): a single markdown doc that documents the Meta developer app config, requested permissions/scopes, test-Page + test-user setup, and a screencast checklist needed for production Facebook Page publishing readiness.

Purpose: FB-01 is a documentation/readiness requirement. App review approval (Business Verification, Advanced Access for `pages_manage_posts`) is the gating step before live publishing on Pages the app does not own; this doc captures that path so the team can complete review.
Output: `docs/facebook-app-review-evidence.md`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-facebook-page-publishing-hardening/04-CONTEXT.md
@.planning/phases/04-facebook-page-publishing-hardening/04-RESEARCH.md
@backend/app/facebook_oauth.py

Ground the doc in the real code: `REQUIRED_SCOPES = ["pages_show_list", "pages_read_engagement", "pages_manage_posts"]`, the env-driven `oauth_config()` (FACEBOOK_APP_ID/SECRET/REDIRECT_URI/RETURN_URL), and the existing redacted-diagnostics guarantee (tokens never serialized to clients) which satisfies App Review "no token leakage" concerns.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Meta app-review evidence document</name>
  <files>docs/facebook-app-review-evidence.md</files>
  <action>Create `docs/facebook-app-review-evidence.md` with these sections, grounded in `facebook_oauth.py` (cite real symbols, do not invent endpoints): (1) **App configuration** — App ID/Secret supplied via env (`FACEBOOK_APP_ID`, `FACEBOOK_APP_SECRET`), redirect URI (`FACEBOOK_REDIRECT_URI`, default `http://127.0.0.1:8787/api/v1/facebook/oauth/callback`), return URL, Graph version v25.0, and the dialog/OAuth code flow. (2) **Permissions & scopes** — table of `pages_show_list`, `pages_read_engagement`, `pages_manage_posts` with: what each is used for in LocalPilot, standard vs advanced access, and the explicit note that `pages_manage_posts` on Pages the app does not own requires **App Review + Business Verification + Advanced Access**; Page admins / test users get standard access without review. (3) **Test Page & test user setup** — steps to create a Meta test app/user, add a Page the test user admins, and confirm `tasks` includes `CREATE_CONTENT`/`MANAGE`. (4) **Screencast checklist** — ordered steps to demonstrate for review: Connect Facebook → choose Page (picker) → approve a draft → publish text post → publish link post → publish single image, each with the expected on-screen result (post ID / permalink). (5) **Data handling / privacy evidence** — state that access tokens are stored server-side encrypted (Fernet, env key — see Phase 4 token store) and never returned to the browser; only redacted token-boundary refs/fingerprints are serialized (reference `token_boundary.serialize_token_boundary_ref`). (6) **Open items / blockers** — Business Verification status, Advanced Access approval, and that live publishing remains manual-verify until a real app is approved. Keep it plain and checklist-driven; mark it the canonical FB-01 artifact.</action>
  <verify>
    <automated>test -f docs/facebook-app-review-evidence.md &amp;&amp; grep -q "pages_manage_posts" docs/facebook-app-review-evidence.md &amp;&amp; grep -qi "business verification" docs/facebook-app-review-evidence.md &amp;&amp; grep -qi "screencast" docs/facebook-app-review-evidence.md</automated>
  </verify>
  <done>`docs/facebook-app-review-evidence.md` exists, names all three scopes, flags `pages_manage_posts` review requirements, includes a connect→select→approve→publish screencast checklist, and documents the encrypted/redacted token handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc → reviewer | Documentation only; no executable code path. The only risk is leaking real secrets into a committed doc. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-DOC | Information Disclosure | `docs/facebook-app-review-evidence.md` | mitigate | Reference env var NAMES only; never paste real App Secret, tokens, or test credentials into the doc |
| T-04-02-SC | Tampering | dependencies | accept | No package installs in this plan (docs only) |
</threat_model>

<verification>
- `docs/facebook-app-review-evidence.md` present and ≥ 60 lines.
- Contains all three scope names and the App Review / Business Verification note.
- No real `FACEBOOK_APP_SECRET` value or access token literal in the file.
</verification>

<success_criteria>
- The team has a single, code-grounded FB-01 artifact that maps the production app-review path (config, scopes, test Page, screencast, data handling).
</success_criteria>

<artifacts_this_phase_produces>
- File: `docs/facebook-app-review-evidence.md` (new — FB-01 evidence artifact).
- No new code symbols, endpoints, tables, or env vars (documents existing `FACEBOOK_APP_ID/SECRET/REDIRECT_URI` usage).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-facebook-page-publishing-hardening/04-02-SUMMARY.md` when done.
</output>
