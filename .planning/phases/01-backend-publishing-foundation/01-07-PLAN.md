---
phase: 01-backend-publishing-foundation
plan: 07
type: execute
wave: 5
depends_on:
  - 01-05
files_modified:
  - backend/app/server.py
  - backend/app/store.py
  - backend/app/contracts.py
  - backend/tests/test_debug_redaction.py
  - scripts/smoke-debug-route.mjs
  - package.json
autonomous: true
requirements:
  - FOUND-02
  - SEC-03
  - SEC-06
  - APPR-06
  - STATUS-01
  - STATUS-02
user_setup: []
must_haves:
  truths:
    - "Backend exposes read-only debug diagnostics for publish jobs, attempts, statuses, error classes, trace IDs, redacted diagnostics, next action, approver, draft version, timestamps, and media refs per D-20, D-21, and D-22."
    - "Debug API exposes only redacted provider diagnostics and redacted token boundary refs per SEC-03 and SEC-06."
    - "Debug endpoint reads workflow records from Plans 01 through 05 and introduces no mutation behavior or role/permission model per D-23."
  artifacts:
    - path: "backend/tests/test_debug_redaction.py"
      provides: "Debug endpoint redaction coverage"
    - path: "scripts/smoke-debug-route.mjs"
      provides: "Full-stack debug endpoint smoke"
    - path: "backend/app/server.py"
      provides: "Read-only `/api/v1/debug/publish-jobs` route"
      contains: "debug/publish-jobs"
  key_links:
    - from: "backend/app/server.py"
      to: "backend/app/contracts.py"
      via: "redacted debug serialization"
      pattern: "debug|redact"
    - from: "backend/app/server.py"
      to: "backend/app/store.py"
      via: "publish job diagnostics query"
      pattern: "debug.*publish"
    - from: "scripts/smoke-debug-route.mjs"
      to: "/api/v1/debug/publish-jobs"
      via: "backend smoke request"
      pattern: "debug/publish-jobs"
---

<objective>
Add the backend debug diagnostics API.

Purpose: make support-visible diagnostics useful and safe while keeping debug API work separate from route extraction and React diagnostics UI.

Output: debug API endpoint, support-safe serializers, debug redaction tests, and debug endpoint smoke checks.
</objective>

## Phase Goal

**As a** LocalPilot operator, **I want to** fetch redacted publishing diagnostics from the backend, **so that** support can diagnose fake publish attempts without exposing secrets.

<execution_context>
@/Users/huijie/.codex/gsd-core/workflows/execute-plan.md
@/Users/huijie/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-backend-publishing-foundation/01-CONTEXT.md
@.planning/phases/01-backend-publishing-foundation/01-RESEARCH.md
@.planning/phases/01-backend-publishing-foundation/01-UI-SPEC.md
@.planning/phases/01-backend-publishing-foundation/01-PATTERNS.md
@.planning/phases/01-backend-publishing-foundation/01-05-SUMMARY.md
</context>

## Artifacts this phase produces

- `GET /api/v1/debug/publish-jobs` in `backend/app/server.py`
- Debug query and safe support serializer in backend store/contracts
- `backend/tests/test_debug_redaction.py` and `scripts/smoke-debug-route.mjs`
- `package.json` script `test:debug`

<tasks>

<task type="auto">
  <name>Task 1: Add failing debug endpoint smoke and redaction tests</name>
  <files>backend/tests/test_debug_redaction.py, scripts/smoke-debug-route.mjs, package.json</files>
  <read_first>
    @.planning/phases/01-backend-publishing-foundation/01-CONTEXT.md
    @.planning/phases/01-backend-publishing-foundation/01-UI-SPEC.md
    @.planning/phases/01-backend-publishing-foundation/01-RESEARCH.md
    @.planning/phases/01-backend-publishing-foundation/01-05-SUMMARY.md
    @.planning/codebase/CONCERNS.md
  </read_first>
  <action>
    Create `backend/tests/test_debug_redaction.py` to require `GET /api/v1/debug/publish-jobs` and assert each row includes merchant, platform, job status, attempt count, latest trace ID, error class, updated timestamp, next action, approver, draft version, media refs, tokenBoundaryRef, approval snapshot summary, attempts, append-only events, and redacted diagnostics per D-20, D-21, and D-22. Create `scripts/smoke-debug-route.mjs` to run the backend, produce one published and one retry-needed fake job, call the debug endpoint, and fail if forbidden secret-like values appear. Add `test:debug` to `package.json`.
  </action>
  <verify>
    <automated>python3 -m unittest backend.tests.test_debug_redaction -v</automated>
    <automated>npm run test:debug</automated>
  </verify>
  <acceptance_criteria>
    - Debug tests fail before the endpoint exists.
    - Debug rows include approver, draft version, trace ID, attempt count, error class, updated timestamp, and next action.
    - Debug payloads include media refs and redacted tokenBoundaryRef only.
    - Debug smoke fails if any response contains token-like or raw provider diagnostic values.
  </acceptance_criteria>
  <done>Debug tests define the support diagnostics contract and redaction requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Implement read-only redacted debug API</name>
  <files>backend/app/server.py, backend/app/store.py, backend/app/contracts.py</files>
  <read_first>
    @backend/app/server.py
    @backend/app/store.py
    @backend/app/contracts.py
    @.planning/phases/01-backend-publishing-foundation/01-CONTEXT.md
    @.planning/phases/01-backend-publishing-foundation/01-RESEARCH.md
    @.planning/phases/01-backend-publishing-foundation/01-05-SUMMARY.md
  </read_first>
  <action>
    Add `GET /api/v1/debug/publish-jobs` per D-20, D-21, D-22, and D-23. The response must be support-safe and include publish jobs, attempts, statuses, trace IDs, error classes, redacted provider diagnostics, next recommended action, timestamps, approver, draft version, media refs, and redacted token boundary refs. The endpoint is read-only, uses backend records created in Plans 01 through 05, and returns sanitized errors without leaking token boundary internals or raw provider diagnostics.
  </action>
  <verify>
    <automated>python3 -m unittest backend.tests.test_publish_approval backend.tests.test_token_boundary backend.tests.test_fake_publish_lifecycle backend.tests.test_retry_redaction_idempotency backend.tests.test_debug_redaction -v</automated>
    <automated>npm run test:debug</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/v1/debug/publish-jobs` returns rows for published and retry-needed fake jobs.
    - Debug serializers include approval snapshot summary, attempts, append-only events, media refs, and redacted diagnostics.
    - Debug serializers do not include OAuth secrets, refresh tokens, app secrets, API keys, provider credentials, cookies, raw OAuth payloads, authorization headers, or token values.
    - Debug endpoint introduces no mutation behavior and no role/permission model.
  </acceptance_criteria>
  <done>Backend exposes read-only redacted diagnostics for support inspection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend debug endpoint | Support-visible publishing records are exposed through a read-only API. |
| Token boundary refs to debug API | Server-only token boundary internals are serialized as redacted refs only. |
| Diagnostics to debug API | Provider-like diagnostics are sanitized before support serialization. |
| Supply chain | Debug API and smoke tests use existing local dependencies only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-S | Spoofing | Debug endpoint | accept | Phase 1 debug API exposes read-only local pilot diagnostics and grants no provider capabilities or secrets. |
| T-01-07-T | Tampering | Debug events and attempts | mitigate | Debug endpoint reads append-only backend records; no debug mutation endpoint is introduced. |
| T-01-07-R | Repudiation | Support diagnostics | mitigate | Debug rows show approver, draft version, timestamps, trace IDs, attempts, and events for auditability per SEC-06. |
| T-01-07-I | Information Disclosure | Debug API | mitigate | Debug serializers expose redacted provider diagnostics and redacted tokenBoundaryRef only. |
| T-01-07-D | Denial of Service | Debug endpoint | accept | Debug endpoint reads bounded local pilot records in Phase 1 and returns safe errors. |
| T-01-07-E | Elevation of Privilege | Debug API | mitigate | Endpoint is read-only and cannot approve, publish, retry, or change provider targets. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs; dependency lists remain unchanged. |
</threat_model>

<verification>
Run `python3 -m unittest backend.tests.test_debug_redaction -v` and `npm run test:debug`.
</verification>

<success_criteria>
- Debug API returns safe diagnostics with trace IDs, error classes, attempts, events, approver, draft version, media refs, timestamps, and redacted provider details.
- Debug API never exposes token boundary internals, OAuth payloads, raw provider data, or credentials.
- Debug endpoint is read-only and does not add a role/permission model.
- Plan owns 6 modified files, keeping debug API below the checker warning threshold.
</success_criteria>

<output>
Create `.planning/phases/01-backend-publishing-foundation/01-07-SUMMARY.md` when done.
</output>
