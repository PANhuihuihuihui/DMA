---
phase: 01-backend-publishing-foundation
plan: 06
type: execute
wave: 5
depends_on:
  - 01-04
  - 01-05
files_modified:
  - src/api/publishingClient.js
  - src/models/publishing.js
  - src/components/PublishTimeline.jsx
  - src/components/RetryPublishControl.jsx
  - src/main.jsx
  - src/styles.css
autonomous: true
requirements:
  - FOUND-02
  - FOUND-05
  - FOUND-06
  - SEC-03
  - STATUS-02
user_setup: []
must_haves:
  truths:
    - "Merchant can retry a failed or retry-needed fake publish job through `Retry publish` per STATUS-02, D-18, and D-19."
    - "`Retry publish` is only visible for `failed` and `retry_needed` jobs and never for draft, review, queued, publishing, or published states."
    - "Timeline UI preserves prior attempts and appends new retry rows without exposing token, secret, cookie, authorization, OAuth, or provider credential values per SEC-03."
  artifacts:
    - path: "src/components/RetryPublishControl.jsx"
      provides: "Retry publish control and confirmation"
      exports: ["RetryPublishControl"]
    - path: "src/components/PublishTimeline.jsx"
      provides: "Attempt-grouped timeline rendering"
      exports: ["PublishTimeline"]
    - path: "src/api/publishingClient.js"
      provides: "Retry API client method"
      exports: ["retryPublishJob"]
  key_links:
    - from: "src/components/RetryPublishControl.jsx"
      to: "src/api/publishingClient.js"
      via: "retryPublishJob"
      pattern: "retryPublishJob"
    - from: "src/main.jsx"
      to: "src/components/RetryPublishControl.jsx"
      via: "selected platform retryable job status"
      pattern: "RetryPublishControl"
    - from: "src/components/PublishTimeline.jsx"
      to: "src/models/publishing.js"
      via: "attempt grouping and redacted diagnostics"
      pattern: "attempt"
---

<objective>
Wire idempotent retry into the React timeline UI.

Purpose: let merchants trigger backend-safe retries and see immutable attempt history while keeping retry UI work separate from the backend retry/idempotency implementation.

Output: retry API client method, retry control component, attempt-grouped timeline updates, `/app` retry wiring, and responsive retry/timeline styles.
</objective>

## Phase Goal

**As a** local business merchant, **I want to** retry a failed fake publish job from the timeline, **so that** I can see a new attempt recorded without losing prior attempt history.

<execution_context>
@/Users/huijie/.codex/gsd-core/workflows/execute-plan.md
@/Users/huijie/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-backend-publishing-foundation/01-CONTEXT.md
@.planning/phases/01-backend-publishing-foundation/01-RESEARCH.md
@.planning/phases/01-backend-publishing-foundation/01-UI-SPEC.md
@.planning/phases/01-backend-publishing-foundation/01-PATTERNS.md
@.planning/phases/01-backend-publishing-foundation/01-04-SUMMARY.md
@.planning/phases/01-backend-publishing-foundation/01-05-SUMMARY.md
@src/main.jsx
@src/styles.css
</context>

## Artifacts this phase produces

- `src/api/publishingClient.js` exports `retryPublishJob`
- `src/components/RetryPublishControl.jsx` renders `Retry publish` with UI-SPEC confirmation behavior
- `src/components/PublishTimeline.jsx` groups events by attempt number while preserving chronological rows
- `src/main.jsx` renders retry beside the selected platform timeline only when backend job status allows it
- `src/styles.css` adds retry/timeline fit styles under `.publish-timeline-*`

<tasks>

<task type="auto">
  <name>Task 1: Add retry client method and attempt-aware timeline models</name>
  <files>src/api/publishingClient.js, src/models/publishing.js, src/components/PublishTimeline.jsx</files>
  <read_first>
    @src/api/publishingClient.js
    @src/models/publishing.js
    @src/components/PublishTimeline.jsx
    @.planning/phases/01-backend-publishing-foundation/01-04-SUMMARY.md
    @.planning/phases/01-backend-publishing-foundation/01-05-SUMMARY.md
  </read_first>
  <action>
    Extend `src/api/publishingClient.js` with `retryPublishJob(jobId)` calling `POST /api/v1/publish-jobs/{job_id}/retry`. Update `src/models/publishing.js` and `PublishTimeline.jsx` so timeline rendering groups events by attempt number, preserves chronological rows, keeps previous attempt rows visible after retry, and renders only backend-redacted diagnostics per SEC-03. The UI model must treat backend status as authoritative and must not infer retry eligibility from client-only labels.
  </action>
  <verify>
    <automated>npm run test:approval</automated>
    <automated>npm run test:fake-publish</automated>
    <automated>npm run test:retry-redaction</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `retryPublishJob` does not accept or send alternate draft payloads, provider targets, token values, or diagnostic overrides.
    - Timeline keeps prior attempt rows visible after retry and appends the new attempt rows.
    - Attempt grouping handles attempt numbers, trace IDs, statuses, timestamps, and next action labels from backend responses.
    - No rendered diagnostic field contains forbidden token, secret, cookie, authorization, OAuth, or provider credential values.
  </acceptance_criteria>
  <done>Frontend can call the backend retry endpoint and render attempt history safely.</done>
</task>

<task type="auto">
  <name>Task 2: Add retry control and `/app` retry wiring</name>
  <files>src/components/RetryPublishControl.jsx, src/main.jsx, src/styles.css</files>
  <read_first>
    @src/main.jsx
    @src/styles.css
    @src/components/PublishTimeline.jsx
    @src/api/publishingClient.js
    @src/models/publishing.js
    @.planning/phases/01-backend-publishing-foundation/01-UI-SPEC.md
    @.planning/phases/01-backend-publishing-foundation/01-PATTERNS.md
  </read_first>
  <action>
    Add `src/components/RetryPublishControl.jsx` that renders `Retry publish` only for backend job statuses `failed` or `retry_needed`, opens a keyboard-reachable confirmation using the UI-SPEC retry copy, disables only the retry button while the API request is pending, and refreshes workflow state after success. In `src/main.jsx`, render retry beside the selected platform timeline only when backend job status allows it and show a toast after retry is accepted. Add `.publish-timeline-*` retry control CSS that uses `--coral` for `retry_needed`, `--red` for `failed`, existing button classes, stable dimensions, and wrapping for trace IDs and idempotency key suffixes.
  </action>
  <verify>
    <automated>npm run test:approval</automated>
    <automated>npm run test:fake-publish</automated>
    <automated>npm run test:retry-redaction</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `Retry publish` is absent for `draft`, `needs_review`, `approved`, `queued`, `publishing`, and `published`.
    - Confirmation copy states that LocalPilot creates a new attempt for the approved snapshot and uses idempotency to avoid duplicate published outcomes.
    - Retry failures preserve the prior attempt rows and append the new failure when the backend records one.
    - Mobile and desktop CSS keeps lifecycle labels, CTA text, trace IDs, and error classes inside their containers.
  </acceptance_criteria>
  <done>Merchant can safely retry failed fake publish jobs and see immutable attempt history in the timeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to retry endpoint | Merchant requests another fake attempt for a failed job. |
| Retry API response to React | Backend retry result becomes timeline and retry-control state. |
| Diagnostics to UI | Provider-like diagnostics are displayed only after backend redaction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-S | Spoofing | Retry control | mitigate | UI sends only backend job IDs; backend from Plan 05 decides retry eligibility and outcome. |
| T-01-06-T | Tampering | Attempt history display | mitigate | Timeline renders backend attempt rows and never mutates or collapses previous attempt history. |
| T-01-06-R | Repudiation | Retry UI feedback | mitigate | Show backend attempt number, trace ID, timestamps, statuses, and next action in the timeline. |
| T-01-06-I | Information Disclosure | Retry diagnostics in React | mitigate | Render backend-redacted diagnostics only and fail build/tests if forbidden values are present in fixtures. |
| T-01-06-D | Denial of Service | Retry request state | accept | Disable only the retry button during the bounded local request and keep navigation usable. |
| T-01-06-E | Elevation of Privilege | Browser API client | mitigate | Client cannot change approval snapshot, provider target, connected channel, or idempotency key. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs; dependency lists remain unchanged. |
</threat_model>

<verification>
Run `npm run test:approval`, `npm run test:fake-publish`, `npm run test:retry-redaction`, and `npm run build`.
</verification>

<success_criteria>
- The UI exposes `Retry publish` only for retryable states and preserves attempt history.
- Retry uses backend job IDs and backend eligibility, not client-invented payloads.
- React-rendered retry and timeline diagnostics contain no forbidden token, secret, cookie, authorization, OAuth, or provider credential values.
- Plan owns 6 modified files, keeping retry UI below the checker warning threshold.
</success_criteria>

<output>
Create `.planning/phases/01-backend-publishing-foundation/01-06-SUMMARY.md` when done.
</output>
