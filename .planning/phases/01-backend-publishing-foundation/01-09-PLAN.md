---
phase: 01-backend-publishing-foundation
plan: 09
type: execute
wave: 7
depends_on:
  - 01-08
files_modified:
  - scripts/smoke-no-publish-localstorage.mjs
  - src/storage/preferences.js
  - src/main.jsx
  - src/styles.css
  - package.json
autonomous: true
requirements:
  - FOUND-01
  - FOUND-06
  - SEC-01
  - SEC-02
  - SEC-03
  - STATUS-02
user_setup: []
must_haves:
  truths:
    - "Publishing-critical records are not persisted through browser localStorage per FOUND-01, FOUND-06, D-05, and D-06."
    - "Browser localStorage contains no campaign records, drafts, approval snapshots, media asset records, publish jobs, attempts, events, diagnostics, token boundary internals, provider tokens, or credentials per SEC-01, SEC-02, and SEC-03."
    - "The existing `/`, `/app`, `/app?module=calendar`, and `/app/debug` routes compile and package checks still work."
    - "Final phase gates verify approval, fake publish, retry, debug, storage boundary, build, and package behavior."
  artifacts:
    - path: "scripts/smoke-no-publish-localstorage.mjs"
      provides: "Static browser storage boundary smoke"
    - path: "src/storage/preferences.js"
      provides: "Allowed preference storage allowlist"
    - path: "package.json"
      provides: "Aggregate test scripts and final verification commands"
  key_links:
    - from: "scripts/smoke-no-publish-localstorage.mjs"
      to: "src/main.jsx"
      via: "source scan excluding comments"
      pattern: "localStorage"
    - from: "src/main.jsx"
      to: "src/storage/preferences.js"
      via: "allowed preference helpers"
      pattern: "readPreference|writePreference"
    - from: "package.json"
      to: "scripts/smoke-no-publish-localstorage.mjs"
      via: "test:storage-boundary"
      pattern: "test:storage-boundary"
---

<objective>
Complete Phase 1 with browser storage guardrails and final verification gates.

Purpose: prove publish-critical state lives behind backend APIs, only low-risk preferences remain in browser localStorage, retry/idempotency behavior stays intact, and the existing static build/package path still works.

Output: no-publish-localStorage smoke test, preference allowlist cleanup, reset behavior update, aggregate `npm test`, and final build/package verification.
</objective>

## Phase Goal

**As a** LocalPilot merchant and operator, **I want to** keep backend publishing records separate from browser preferences, **so that** approval, retry, media, token-boundary, and diagnostics records remain safe and auditable.

<execution_context>
@/Users/huijie/.codex/gsd-core/workflows/execute-plan.md
@/Users/huijie/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-backend-publishing-foundation/01-CONTEXT.md
@.planning/phases/01-backend-publishing-foundation/01-RESEARCH.md
@.planning/phases/01-backend-publishing-foundation/01-UI-SPEC.md
@.planning/phases/01-backend-publishing-foundation/01-PATTERNS.md
@.planning/phases/01-backend-publishing-foundation/01-08-SUMMARY.md
@src/main.jsx
@src/styles.css
@src/storage/preferences.js
</context>

## Artifacts this phase produces

- `scripts/smoke-no-publish-localstorage.mjs` scans non-comment frontend source for forbidden publish-critical localStorage writes
- `src/storage/preferences.js` remains the only browser storage boundary for allowed low-risk preferences
- `src/main.jsx` reset and persistence paths use backend reloads for workflow records and preference helpers for UI state
- `package.json` adds `test:storage-boundary` and aggregate `test` gates for backend unittests, smoke scripts, build, and package readiness
- `src/styles.css` receives only any final responsive fixes needed for the approved Phase 1 UI surfaces

<tasks>

<task type="auto">
  <name>Task 1: Add storage-boundary smoke test</name>
  <files>scripts/smoke-no-publish-localstorage.mjs, package.json</files>
  <read_first>
    @.planning/phases/01-backend-publishing-foundation/01-CONTEXT.md
    @.planning/phases/01-backend-publishing-foundation/01-RESEARCH.md
    @.planning/codebase/CONCERNS.md
    @src/main.jsx
    @src/storage/preferences.js
  </read_first>
  <action>
    Create `scripts/smoke-no-publish-localstorage.mjs` to statically inspect frontend source files and fail when non-comment code writes `localpilot-demo-input`, `localpilot-demo-plans`, `localpilot-export-package`, approval snapshots, media asset records, publish jobs, publish attempts, publish events, diagnostics, provider token boundary internals, OAuth payloads, provider credentials, cookies, authorization headers, or token-like diagnostics to `window.localStorage` or `localStorage`. Use source parsing or line filtering that ignores comments and does not rely on bare unfiltered `grep -c`. Add `test:storage-boundary` to `package.json`.
  </action>
  <verify>
    <automated>npm run test:storage-boundary</automated>
  </verify>
  <acceptance_criteria>
    - Storage smoke permits `localpilot-language`, `localpilot-demo-session`, `localpilot-demo-active-module`, `localpilot-demo-selected-channel`, `localpilot-demo-selected-post`, `localpilot-demo-selected-inbox`, and `localpilot-demo-selected-walkthrough`.
    - Storage smoke fails if publish-critical records, media asset records, token boundary internals, or diagnostic records are written to browser storage.
    - Storage smoke ignores comments and string mentions in planning/docs outside scanned frontend source.
    - `package.json` adds scripts only.
  </acceptance_criteria>
  <done>Storage-boundary tests define the final browser persistence guardrail.</done>
</task>

<task type="auto">
  <name>Task 2: Remove publish-critical localStorage writes and preserve preference behavior</name>
  <files>src/storage/preferences.js, src/main.jsx, src/styles.css</files>
  <read_first>
    @src/main.jsx
    @src/styles.css
    @src/storage/preferences.js
    @.planning/phases/01-backend-publishing-foundation/01-CONTEXT.md
    @.planning/phases/01-backend-publishing-foundation/01-UI-SPEC.md
  </read_first>
  <action>
    Finish the D-05 and D-06 localStorage boundary by routing only allowed UI preferences through `src/storage/preferences.js`: language, demo session, active module, selected channel, selected post, selected inbox, and selected walkthrough. Remove or ignore code that writes publish-critical campaignInput, generated plans, approval snapshots, media asset records, publish jobs, publish attempts, publish events, diagnostics, token boundary internals, and export packages to browser localStorage. Make `resetDemo` clear only safe demo preferences and reload seeded backend workflow records without deleting backend audit records. Apply only final CSS fit fixes needed for timeline, retry, snapshot, or debug surfaces under the approved Phase 1 class prefixes.
  </action>
  <verify>
    <automated>npm run test:storage-boundary</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Static storage boundary smoke finds no non-comment localStorage write for publish-critical records.
    - Current low-risk preferences still persist safely.
    - Reset behavior reloads backend workflow records and does not delete backend audit rows.
    - `/`, `/app`, `/app?module=calendar`, and `/app/debug` compile.
  </acceptance_criteria>
  <done>Browser storage is limited to safe preferences while publishing records remain backend-owned.</done>
</task>

<task type="auto">
  <name>Task 3: Add aggregate final verification gates</name>
  <files>package.json</files>
  <read_first>
    @package.json
    @scripts/package-sites.mjs
    @scripts/prepare-sites-dist.mjs
    @.planning/codebase/TESTING.md
    @.planning/phases/01-backend-publishing-foundation/01-08-SUMMARY.md
  </read_first>
  <action>
    Update the aggregate `test` script so it runs backend unittest discovery, `npm run test:approval`, `npm run test:fake-publish`, `npm run test:retry-redaction`, `npm run test:debug`, `npm run test:storage-boundary`, and `npm run build`. Keep `npm run package:sites` as the final deployment package gate and do not make it depend on backend secrets. Preserve existing `build`, `preview`, and `package:sites` commands.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>npm run package:sites</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` runs backend lifecycle, token-boundary, redaction, idempotency, debug, storage, and build checks.
    - `npm run package:sites` still produces the deployable frontend archive and does not require backend secrets.
    - Existing static package output path remains unchanged unless `SITES_ARCHIVE_PATH` is explicitly set.
    - `.vscode/`, generated `dist/`, and unrelated existing user changes are not modified by this task.
  </acceptance_criteria>
  <done>Phase 1 has final automated gates covering backend workflow, token boundary, media refs, redaction, storage guardrails, build, and packaging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage boundary | Browser storage remains allowed only for low-risk demo preferences and session convenience. |
| Backend workflow API to browser | Publish-critical records remain in backend APIs and are not persisted client-side. |
| Build/package boundary | Static frontend packaging remains separate from the local backend process and secrets. |
| Supply chain | Final gates run without new package-manager installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09-S | Spoofing | Preference storage | accept | Preferences are UI conveniences and do not authorize provider actions or approval authority. |
| T-01-09-T | Tampering | Browser localStorage | mitigate | Publish-critical records are not read from or written to localStorage; backend remains source of truth. |
| T-01-09-R | Repudiation | Browser reset behavior | mitigate | Reset clears preferences only and reloads backend audit records instead of deleting approval/job history. |
| T-01-09-I | Information Disclosure | localStorage, build output, package output | mitigate | Storage smoke blocks campaign/draft/approval/media/job/attempt/event/diagnostic/token-boundary data and secret-like fields from browser storage. |
| T-01-09-D | Denial of Service | Aggregate test script | accept | Final gates are local developer commands and do not affect runtime availability. |
| T-01-09-E | Elevation of Privilege | Static package | mitigate | Static package contains frontend assets only and no backend secrets or provider token boundary internals. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs; dependency lists remain unchanged. |
</threat_model>

<verification>
Run `npm run test:storage-boundary`, `npm test`, `npm run build`, and `npm run package:sites`.
</verification>

<success_criteria>
- Browser localStorage no longer owns publish-critical campaign, draft, approval, media, job, attempt, event, diagnostics, token boundary, or export records.
- Final verification proves no forbidden secret-like data is stored, logged, returned, rendered, or committed by Phase 1 code.
- `/`, `/app`, and `/app/debug` compile and the existing static package path still works.
- Aggregate gates cover approval, media assets, token boundary, fake publish, retry, redaction, debug, storage, build, and packaging.
- Plan owns 5 modified files, keeping final storage and verification gates below the checker warning threshold.
</success_criteria>

<output>
Create `.planning/phases/01-backend-publishing-foundation/01-09-SUMMARY.md` when done.
</output>
