---
phase: 06-manual-fallback-and-pilot-support
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - backend/app/store.py
  - backend/app/server.py
  - src/api/publishingClient.js
  - backend/tests/test_evidence_export.py
autonomous: true
requirements: [ADMIN-04]
must_haves:
  truths:
    - "An operator can export a per-job redacted evidence bundle for Meta/TikTok app review and pilot troubleshooting"
    - "The evidence bundle composes existing data: merchant, channel, job, attempts, events, and diagnostics plus app-review fields (scopes, chosen route, Direct Post gate results, creator/disclosure confirmations)"
    - "The evidence export contains no tokens or secrets"
    - "The evidence export is gated to operators via the same role gating as the rest of the admin namespace"
  artifacts:
    - path: "backend/app/store.py"
      provides: "Per-job redacted evidence bundle builder"
      contains: "evidence"
    - path: "backend/tests/test_evidence_export.py"
      provides: "Evidence export coverage and redaction assertions"
      contains: "evidence"
  key_links:
    - from: "backend/app/server.py"
      to: "backend/app/store.py"
      via: "operator evidence export endpoint"
      pattern: "evidence"
    - from: "src/api/publishingClient.js"
      to: "backend/app/server.py"
      via: "evidence export client call"
      pattern: "evidence"
---

<objective>
Add a per-job redacted app-review/pilot evidence export (ADMIN-04) that bundles existing job data plus app-review-relevant fields into one downloadable JSON, gated to operators and free of tokens/secrets.

Purpose: produce a single artifact an operator can attach to a Meta/TikTok app-review submission or a pilot troubleshooting ticket, reusing data already captured upstream.
Output: an evidence-bundle builder in store, an operator endpoint, a client call, and tests.
</objective>

<execution_context>
@/Users/huijie/.codex/gsd-core/workflows/execute-plan.md
@/Users/huijie/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-manual-fallback-and-pilot-support/06-CONTEXT.md
@.planning/phases/06-manual-fallback-and-pilot-support/06-01-PLAN.md
@.planning/phases/06-manual-fallback-and-pilot-support/06-02-PLAN.md
@backend/app/store.py
@backend/app/server.py
@src/api/publishingClient.js

App-review-relevant data already exists in the approval snapshot and attempt diagnostics: TikTok route + Direct Post gate results (`diagnostics.route`, `diagnostics.directPost`), TikTok creator/disclosure confirmations (`snapshot.creatorInfoSnapshot`, `snapshot.tiktokConfirmations`), and Facebook permission/error classes. Reuse `serialize_debug_publish_job`, `summarize_approval_snapshot`, and `safe_diagnostics`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a per-job redacted evidence bundle in store.py</name>
  <files>backend/app/store.py, backend/tests/test_evidence_export.py</files>
  <read_first>
    - .planning/phases/06-manual-fallback-and-pilot-support/06-CONTEXT.md (D-08)
    - backend/app/store.py (serialize_debug_publish_job, summarize_approval_snapshot, safe_diagnostics)
    - backend/app/contracts.py (redact, safe_diagnostics, summarize_approval_snapshot)
  </read_first>
  <action>
  Add `build_publish_job_evidence(conn, job_id)` that composes a single redacted dict: `job` (from `serialize_debug_publish_job`), `merchant` and `connectedChannel` summaries, `attempts` and `events` (already on the serialized job), and an `appReview` section extracting from the approval snapshot + latest attempt diagnostics: chosen publish `route`/`deliveryMode`, `directPost` gate result (checks + blockedReasons), TikTok `creatorInfoSnapshot` summary and `tiktokConfirmations`, and the terminal `errorClass`/`nextRecommendedAction`. Wrap the entire return in `safe_diagnostics`/`redact` so no tokens/secrets can leak. Add an `exportedAt` timestamp via `utc_now()`. Raise `StoreError(404)` if the job does not exist.
  </action>
  <verify>
    <automated>python3 -m unittest backend.tests.test_evidence_export -v</automated>
  </verify>
  <acceptance_criteria>
    - `build_publish_job_evidence` returns a dict with `job`, `appReview`, and `exportedAt` keys.
    - For a TikTok job, `appReview` includes `route`/`deliveryMode` and the Direct Post gate result; for a job with confirmations, it includes the creator/disclosure confirmation summary.
    - A forbidden-term scan over the JSON-serialized bundle finds no access_token/refresh_token/secret.
    - Unknown job id raises StoreError 404.
  </acceptance_criteria>
  <done>A redacted, app-review-ready evidence bundle can be built from existing job data.</done>
</task>

<task type="auto">
  <name>Task 2: Expose the evidence export via an operator endpoint and client call</name>
  <files>backend/app/server.py, src/api/publishingClient.js, backend/tests/test_evidence_export.py</files>
  <read_first>
    - backend/app/server.py (admin routes, resolve_operator_context, match_admin_publish_job from 06-02)
    - backend/app/store.py (build_publish_job_evidence, resolve_operator)
    - src/api/publishingClient.js (admin client call patterns from 06-02)
  </read_first>
  <action>
  Add `GET /api/v1/admin/publish-jobs/{id}/evidence` to the admin namespace, gated by the same `resolve_operator_context` used by the other admin routes, returning `{ "evidence": store.build_publish_job_evidence(conn, id) }`. Extend `match_admin_publish_job` (or its action handling) to recognize the `evidence` action. Mirror it in `src/api/publishingClient.js` as `loadAdminPublishJobEvidence(jobId)`.
  </action>
  <verify>
    <automated>python3 -m unittest backend.tests.test_evidence_export -v</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/v1/admin/publish-jobs/{id}/evidence` returns the evidence bundle for an operator session and 403 for a non-operator.
    - The endpoint response passes a forbidden-term scan.
    - publishingClient.js exports `loadAdminPublishJobEvidence`.
  </acceptance_criteria>
  <done>Operators can export a per-job redacted evidence bundle over the admin namespace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> evidence export | Evidence bundles must be operator-gated and contain no secrets |
| stored job data -> evidence bundle | Composition must not surface raw provider payloads or tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure | evidence bundle | mitigate | Wrap the whole bundle in `safe_diagnostics`/`redact`; compose only already-redacted serializers |
| T-06-03-02 | Elevation of Privilege | evidence endpoint | mitigate | Gate via the shared `resolve_operator_context` (owner/operator role) |
</threat_model>

<verification>
- `python3 -m unittest backend.tests.test_evidence_export -v`
- `python3 -m unittest discover backend/tests` (no regressions)
</verification>

<success_criteria>
- A per-job redacted evidence bundle can be exported by an operator.
- The bundle includes app-review fields (scopes/route/gate results/confirmations) composed from existing data.
- The export leaks no tokens or secrets and is operator-gated.
</success_criteria>

## Artifacts this phase produces
- `store.build_publish_job_evidence(conn, job_id)` (function)
- `GET /api/v1/admin/publish-jobs/{id}/evidence` (route)
- `loadAdminPublishJobEvidence(jobId)` (client function)
- `backend/tests/test_evidence_export.py` (test module)

<output>
Create `.planning/phases/06-manual-fallback-and-pilot-support/06-03-SUMMARY.md` when done.
</output>
