---
phase: 07-google-login-and-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/main.jsx
  - src/api/publishingClient.js
  - index.html
autonomous: true
requirements: [GAUTH-01, GAUTH-02]
must_haves:
  truths:
    - "D-08: The prototype fake login in src/main.jsx is replaced by a Google sign-in entry point that posts the Google credential to the backend"
    - "D-05/D-06: The app relies on the backend httpOnly session cookie (requests use credentials: include) rather than a JS-held token, and reads the current user via the session endpoint"
    - "D-07: A dev-only login control is shown only when the backend dev bypass is active, so local/demo use still works without Google credentials"
  artifacts:
    - path: "src/main.jsx"
      provides: "Google sign-in entry replacing the fake login, with gated dev bypass"
      contains: "google"
    - path: "src/api/publishingClient.js"
      provides: "Cookie-based auth client calls used by the login UI"
      contains: "googleLogin"
  key_links:
    - from: "src/main.jsx"
      to: "src/api/publishingClient.js"
      via: "login UI calls googleLogin/devLogin/logout/loadSession"
      pattern: "ogin"
---

<objective>
Replace the prototype fake login in the React app with a Google Identity Services sign-in that sends the credential to the backend, relies on the httpOnly session cookie for authentication, and keeps a gated dev-login control for local/demo use. Completes the user-facing side of GAUTH-01/02.

Output: GIS sign-in entry in `src/main.jsx`, cookie-based session wiring, and a conditional dev-login control.
</objective>

<execution_context>
@/Users/huijie/.codex/gsd-core/workflows/execute-plan.md
@/Users/huijie/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-google-login-and-auth/07-CONTEXT.md
@.planning/phases/07-google-login-and-auth/07-01-PLAN.md
@src/main.jsx
@src/api/publishingClient.js
@index.html

Consumes the auth endpoints and client calls from 07-01 (`googleLogin`, `devLogin`, `logout`, `loadSession`). The existing fake login is a modal in `src/main.jsx`; the public `GOOGLE_CLIENT_ID` is injected via Vite env (e.g. `import.meta.env.VITE_GOOGLE_CLIENT_ID`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the fake login with a Google Identity Services sign-in entry</name>
  <files>src/main.jsx, index.html</files>
  <read_first>
    - .planning/phases/07-google-login-and-auth/07-CONTEXT.md (D-08, D-01)
    - src/main.jsx (existing fake login modal + session handling)
    - index.html (script/document shell)
  </read_first>
  <action>
  Load the Google Identity Services script (in `index.html` or dynamically) and render a Google sign-in button / One Tap that uses `VITE_GOOGLE_CLIENT_ID`. On credential callback, call `googleLogin(credential)` from the publishing client, then load the current session and route into the authenticated workspace. Remove the fake-login form path as the primary login. Keep the existing routes/workspace shell intact. If `VITE_GOOGLE_CLIENT_ID` is absent, hide the Google button and surface the dev-login control (Task 2).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/main.jsx renders a Google sign-in entry that calls `googleLogin` with the returned credential and no longer presents the fake-login form as the primary path.
    - The Google Identity Services library is loaded (index.html or dynamic import) and keyed by `VITE_GOOGLE_CLIENT_ID`.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>The app's primary login is Google sign-in wired to the backend credential endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Use the cookie session app-wide and add a gated dev-login control</name>
  <files>src/main.jsx, src/api/publishingClient.js</files>
  <read_first>
    - .planning/phases/07-google-login-and-auth/07-CONTEXT.md (D-05, D-06, D-07)
    - src/api/publishingClient.js (requestJson — ensure credentials: include)
    - src/main.jsx (session/auth state, logout)
  </read_first>
  <action>
  Ensure the publishing client sends `credentials: "include"` on requests so the httpOnly `lp_session` cookie is transmitted; the app determines auth state via `loadSession()` (no JS-held token, nothing in localStorage). Wire logout to call `logout()` and return to the sign-in screen. Add a dev-login control that calls `devLogin()` and is shown only when the Google client ID is absent (local/demo), mirroring the backend `LOCALPILOT_DEV_LOGIN` gate. Do not store any session token in localStorage.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `requestJson` (or equivalent) issues requests with `credentials: "include"`.
    - The app derives the signed-in user from `loadSession()` and shows the workspace only when a session resolves; no session token is written to localStorage.
    - A dev-login control appears only when `VITE_GOOGLE_CLIENT_ID` is unset and successfully signs in via `devLogin()`.
    - Logout clears the session and returns to the sign-in screen.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>The app authenticates via the httpOnly cookie session, with logout and a gated dev bypass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend auth | The frontend must send the Google credential and rely on the server-set cookie, never a JS-stored token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | session token in JS | mitigate | Use httpOnly cookie + credentials:include; never read/store the token in JS or localStorage |
| T-07-02-02 | Elevation of Privilege | dev-login control | mitigate | Show dev login only when no GOOGLE_CLIENT_ID; backend independently gates the dev path |
</threat_model>

<verification>
- `npm run build`
- Manual/browser smoke: Google button renders with a client ID; dev-login control appears without one.
</verification>

<success_criteria>
- Google sign-in replaces the fake login and authenticates via the backend.
- The app uses the httpOnly cookie session app-wide; logout works.
- A gated dev-login keeps local/demo usable without Google credentials.
</success_criteria>

## Artifacts this phase produces
- Google Identity Services sign-in entry in `src/main.jsx` (replacing the fake login)
- `credentials: "include"` on the publishing client; cookie-session-driven auth state
- Gated dev-login control
- GIS script load in `index.html` (or dynamic) keyed by `VITE_GOOGLE_CLIENT_ID`

<output>
Create `.planning/phases/07-google-login-and-auth/07-02-SUMMARY.md` when done.
</output>
